% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlations.R
\name{recover_correlations}
\alias{recover_correlations}
\title{Score recovered correlations against the generating ones}
\usage{
recover_correlations(
  fits,
  truth,
  estimator = c("model", "draws", "point"),
  scale = c("link", "natural"),
  links = NULL,
  pairs = NULL,
  group = "id",
  ci_level = 0.95,
  ...
)
}
\arguments{
\item{fits}{A fit (a \code{brmsfit}, or any object with an
\code{\link[=extract_subject_draws]{extract_subject_draws()}} method), a list of fits with one element
per replication, or a tibble from \code{\link[=extract_correlations]{extract_correlations()}} that may
carry \code{replication} and \code{condition} columns.}

\item{truth}{A \code{bmmtools_simulation}; a list of them, parallel to
\code{fits}; or a list with the tibbles \code{cor} (\code{term}, \code{true_value}),
\code{subjects} (\code{id}, \code{term}, \code{true_value}) and optionally \code{covariates}
(\code{id}, \code{term}, \code{true_value}), all on the link scale and each of them
optionally carrying \code{replication} or \code{condition}. Without a
\code{replication} column, the same truth applies to every replication.}

\item{estimator}{One or more of \code{"model"}, \code{"draws"} and \code{"point"}.}

\item{scale}{\code{"link"} or \code{"natural"}. When \code{fits} is a tibble and
\code{scale} is not given, the tibble's own scale is used.}

\item{links}{A named character vector mapping a term to a link name.
\code{NULL} reads the link table of a \code{bmmfit}, then of the simulation's
model; without one, the natural scale falls back to the link scale
with a message.}

\item{pairs}{\code{NULL} for every pair in \code{truth}, or a character vector of
pair terms; restricts both the estimates and the truth.}

\item{group}{The grouping factor, \code{"id"} by default.}

\item{ci_level}{The interval mass, passed to \code{\link[=extract_correlations]{extract_correlations()}}.}

\item{...}{Passed to \code{\link[=extract_correlations]{extract_correlations()}}, for example
\code{converged}.}
}
\value{
A \code{bmmtools_cor_recovery} object: a tibble subclass with the
columns \code{term}, \code{var1}, \code{var2}, \code{estimator}, \code{estimate}, \code{ci_low},
\code{ci_high}, \code{ci_method}, \code{ci_level}, \code{rhat}, \code{ess_bulk}, \code{ess_tail},
\code{true_value}, \code{sample_value}, \code{bias}, \code{bias_sample}, \code{covered},
\code{covered_sample}, \code{excludes_zero}, \code{scale}, \code{n}, \code{converged},
\code{condition} and \code{replication}. Call
\link[=summary.bmmtools_cor_recovery]{summary()} on it for the
per-pair metrics and \code{\link[=plot_recovery]{plot_recovery()}} to draw it.
}
\description{
The correlation counterpart of \code{\link[=recover]{recover()}}: one row per replication,
pair and estimator, with the estimate, its interval, the generating
correlation \code{true_value}, the correlation the simulated subjects
actually had \code{sample_value}, the errors against both and whether the
interval excludes zero.
}
\details{
Two truths are carried because they answer different questions. A
finite sample of subjects does not have the generating correlation;
\code{sample_value} is what an estimator could at best recover from these
subjects, and \code{true_value} is what a reader wants to learn about.

\code{bias} is \code{estimate - true_value} and \code{bias_sample} is \code{estimate - sample_value}; \code{covered} and \code{covered_sample} use the closed interval;
\code{excludes_zero} is \code{ci_high < 0 | ci_low > 0}.

\strong{On the natural scale} \code{sample_value} is recomputed from the subject
values after \code{\link[=inverse_link]{inverse_link()}}, covariates untransformed. A generating
correlation on the link scale has no natural-scale counterpart that
one transform gives, so \code{true_value} is \code{0} where the link-scale value
is 0 (independence survives a monotone transform) and \code{NA} otherwise.
The \code{model} estimator has no natural-scale rows; with fits as input
it is dropped with a message.

A truth pair no estimate matches produces a warning and is dropped;
every pair unmatched is an error.
}
\examples{
extracted <- tibble::tibble(
  term = "kappa__thetat", var1 = "kappa", var2 = "thetat",
  estimator = "draws", estimate = c(0.42, 0.18, 0.55),
  ci_low = c(0.1, -0.2, 0.2), ci_high = c(0.7, 0.5, 0.8),
  ci_method = "eti", ci_level = 0.95, rhat = 1, ess_bulk = 800,
  ess_tail = 800, scale = "link", n = 5L, replication = 1:3
)
subjects <- tibble::tibble(
  id = rep(as.character(1:5), 6),
  term = rep(rep(c("kappa", "thetat"), each = 5), 3),
  true_value = c(
    1.2, 0.4, 2.0, 1.1, 0.7, 0.3, -0.5, 0.9, 0.1, -0.2,
    0.8, 1.5, 1.1, 0.2, 1.9, 0.0, 0.6, 0.4, -0.3, 0.8,
    1.4, 0.9, 0.3, 1.8, 1.0, 0.5, 0.2, -0.4, 0.9, 0.1
  ),
  replication = rep(1:3, each = 10)
)
truth <- list(
  cor = tibble::tibble(term = "kappa__thetat", true_value = 0.5),
  subjects = subjects
)
recovery <- recover_correlations(extracted, truth)
summary(recovery)

}
