% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{simulate_recovery}
\alias{simulate_recovery}
\title{Simulate data from a bmm model with known parameters}
\usage{
simulate_recovery(
  model,
  pars,
  n_subjects,
  n_trials,
  sds = NULL,
  cors = NULL,
  covariates = NULL,
  tasks = NULL,
  task_col = "task",
  coding = c("cell", "contrast"),
  contrasts = NULL,
  subject_pars = NULL,
  generator = NULL,
  seed = NULL
)
}
\arguments{
\item{model}{A \code{bmmodel}, built with the column names the fit will
use, so the generated columns match.}

\item{pars}{Named numeric: population values \strong{on the link scale
under bmm's parameter names}, one per estimated parameter. A
fixed parameter may be given too, in which case the generator uses
that value. May also be a function with no arguments returning such
a vector, evaluated under \code{seed}, for random hyperparameters.}

\item{n_subjects, n_trials}{Subjects, and trials per subject and per
row of the generator's layout (for \code{sdt_yn}, per stimulus class).}

\item{sds}{Named numeric: between-subject standard deviations on the
link scale. A parameter not named does not vary. \code{NULL} means no
parameter varies. May be a function, as \code{pars}.}

\item{cors}{A correlation matrix between subject values on the link
scale, with the parameter and covariate names as dimnames; terms it
does not name are uncorrelated. \code{NULL} means none are correlated. May
be a function, as \code{pars}. \code{\link[=cors_from_factors]{cors_from_factors()}} builds one from
factor loadings.}

\item{covariates}{Observed, error-free person variables drawn jointly
with the parameters: a named list of \code{c(mean = , sd = )}. Each
becomes a subject-constant data column after \code{id}. A name must be
syntactic, contain no \verb{_}, and differ from the model's parameters and
columns.}

\item{tasks}{\code{NULL}, or a character vector of at least two task levels
(letters and digits) for a design in which every subject does every
task. Each free parameter then has one value per task, under the term
\verb{<parameter>_<task_col><level>} (\code{kappa_task1}), brms's coefficient
name for \code{0 + task}. See the details.}

\item{task_col}{The name of the task column in \code{data}, \code{"task"} by
default. Used only with \code{tasks}.}

\item{coding}{What the task column means to a fit. \code{"cell"}, the
default, leaves it as it was: a fit of \code{0 + task} estimates one value
per task and the truth is those values. \code{"contrast"} attaches
\code{contrasts} to the task column, so a fit of \code{1 + task} estimates an
intercept and \code{length(tasks) - 1} contrasts, and \code{truth} holds those
instead: \verb{<parameter>} for the intercept and
\verb{<parameter>_<task_col><j>} for the contrasts, with the SD and
correlation truths transformed with them.

\strong{What is drawn does not change.} Subject values are drawn around
the cell means exactly as under \code{"cell"}, from the same RNG stream, so
\code{sds}, \code{cors} and \code{subject_pars} keep their meaning and a simulation
is comparable across codings. Only the design the fit sees, and the
truth it is scored against, differ.}

\item{contrasts}{The contrast matrix for \code{coding = "contrast"}:
\code{length(tasks)} rows and one column fewer, or a function \code{(n)}
returning one. \link[stats:contr.treatment]{stats::contr.treatment} by default, so the intercept
is the first task and each contrast a difference from it. For a study
in which bmm's default prior should mean the same for every task, use
an orthonormal coding such as \code{bayestestR::contr.equalprior}, whose
intercept is the grand mean and whose contrasts are uncorrelated with
it. Ignored under \code{coding = "cell"}.}

\item{subject_pars}{A data frame \code{id}, \code{term}, \code{true_value} (link
scale) of subject values to use instead of drawing them, so that
replications can share the same simulated people. With \code{covariates}
it must give their values too.}

\item{generator}{A function \verb{(pars, n_trials, model)} returning one
subject's rows as a data frame with the model's column names;
\code{pars} is a named list on the natural scale, fixed parameters
included. \code{NULL} uses the adapter bmmtools ships for the model.}

\item{seed}{A seed applied with \code{withr::with_seed()} around the draws
and the generator; \code{NULL} leaves the random number generator alone.}
}
\value{
A list of class \code{bmmtools_simulation} with \code{data} (a tibble,
\code{id} first, then any covariates, then the task column), \code{truth} (a
list of tibbles on the link scale: \code{population} with \code{term} and
\code{true_value}; \code{subjects} with \code{id}, \code{term} and \code{true_value}; \code{sd} with
\code{term} and \code{true_value}; \code{cor} with \code{term}, \code{var1}, \code{var2} and
\code{true_value};
\code{covariates} with \code{id}, \code{term} and \code{true_value}), the realised
\code{pars}, \code{sds}, \code{cors} (the full matrix over varying parameters then
covariates, \code{NULL} with fewer than two) and \code{covariates},
\code{n_subjects}, \code{n_trials}, \code{seed} (\code{NA} when none), \code{model},
\code{generator}, \code{tasks} and \code{task_col} (both \code{NULL} without tasks).
}
\description{
The generate half of the engine: a model plus population values on
the link scale become a data set and the truth that produced it, so
that \code{\link[=recover]{recover()}} and \code{\link[=recover_subjects]{recover_subjects()}} can score a fit of that
data. Subject values are drawn on the link scale around the
population values, converted to the natural scale through
\code{\link[=inverse_link]{inverse_link()}}, and handed to the model's own \verb{r<model>()} generator,
or to the function you pass as \code{generator}, which always takes
precedence over the built-in one.
}
\details{
Adapters exist for \code{sdt_yn}, \code{sdt_mafc}, \code{ezdm} (three parameters),
\code{ddm}, \code{mixture2p} and \code{sdm}. Every other model takes a \code{generator}.
The truth for the subjects and for the SDs lists only the parameters
that vary, because a parameter that does not vary has nothing
person-level to recover.

\strong{Correlated draws.} Subject values and covariates are one
multivariate normal draw, \code{Z \%*\% chol(cors)}, over the varying
parameters in \code{pars} order and then the covariates, scaled by the SDs
afterwards. Without correlations this gives exactly the values an
uncorrelated draw gave in earlier versions, so seeded simulations
stay reproducible; adding a covariate never changes the parameter
values. A correlation pair is named \verb{<a>__<b>}, the two names sorted
in the C locale.

\strong{Tasks.} With \code{tasks}, \code{pars} and \code{sds} may name a parameter, which
gives every task that value, or a full term such as \code{kappa_task2}, which
overrides it for that task; the realised \code{pars} and \code{sds} are stored
with full terms. \code{cors}, \code{subject_pars} and the truth tables use full
terms only, so a correlation between tasks is, for example,
\code{kappa_task1__kappa_task2}. The generator is called once per subject and
task with that task's values under the bare parameter names, and
\code{n_trials} is per task. The task values are cell means: fit them with
\code{recovery_formula(model, task_col = "task")}. Fixed parameters are the
same in every task. Adding tasks changes the random numbers drawn
compared with a simulation without them; \code{tasks = NULL} gives exactly
the simulation of earlier versions.
}
\examples{
\dontrun{
sim <- simulate_recovery(
  bmm::mixture2p(resp_error = "y"),
  pars = c(kappa = log(8), thetat = qlogis(0.75)),
  n_subjects = 30, n_trials = 60,
  sds = c(kappa = 0.3, thetat = 0.5), seed = 1
)
fit <- bmm::bmm(recovery_formula(sim$model), sim$data, sim$model)
recover(fit, sim$truth$population)
recover_subjects(fit, sim$truth$subjects)

# two tasks, kappa lower in the second, correlated .6 across tasks
cors <- diag(2)
dimnames(cors) <- rep(list(c("kappa_task1", "kappa_task2")), 2)
cors[1, 2] <- cors[2, 1] <- 0.6
two_tasks <- simulate_recovery(
  bmm::mixture2p(resp_error = "y"),
  pars = c(kappa = log(8), kappa_task2 = log(5), thetat = qlogis(0.75)),
  n_subjects = 30, n_trials = 60,
  sds = c(kappa_task1 = 0.3, kappa_task2 = 0.3), cors = cors,
  tasks = c("1", "2"), seed = 1
)
formula <- recovery_formula(
  two_tasks$model,
  re_cor = "within", task_col = "task"
)
}

}
