% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior-check.R
\name{prior_check}
\alias{prior_check}
\title{Check what the priors say the data should look like}
\usage{
prior_check(
  model,
  formula,
  data,
  prior = NULL,
  summary = NULL,
  n_draws = 1000,
  range = NULL,
  file = NULL,
  refit = c("on_change", "never", "always"),
  seed = NULL,
  ...,
  .fitter = NULL
)
}
\arguments{
\item{model}{A \code{bmmodel}, built with the column names \code{data} uses.}

\item{formula}{The \code{bmmformula} you intend to fit. The prior a model
needs depends on it, so there is no default.}

\item{data}{The design the prior is checked over. The response column
must exist because brms builds its Stan data from it; its values do
not enter the prior draws, and are used only as the observed
reference in \code{\link[=plot_prior_check]{plot_prior_check()}}.}

\item{prior}{\code{NULL} checks bmm's own defaults. A \code{brmsprior} checks
that one. A \strong{named} list runs one fit per element and compares
them, a \code{NULL} element meaning bmm's defaults, so
\code{list(default = NULL, tight = my_prior)} is the usual comparison.}

\item{summary}{A function \verb{(yrep, data)} returning a data frame with
at least \code{statistic} and \code{value}, and optionally \code{response}. \code{NULL}
uses the default: the share of draws at the floor and at the ceiling
of the response, and the 50th, 90th and 95th percentiles of the
prior-predicted observable.}

\item{n_draws}{Prior-predictive draws kept per prior set. More than
the fit holds is reported and capped, not refused.}

\item{range}{The floor and the ceiling of the response, as two
numbers. \code{NULL} derives them from the model where they are known:
\code{0} and the trial count for \code{sdt_yn} and \code{sdt_mafc}, \code{-pi} and \code{pi}
for \code{mixture2p} and \code{sdm}. Where they are not known the two rates
are \code{NA} rather than invented.}

\item{file}{Where to cache the fits. \code{NULL} uses a temporary file, so
the compile is reused within the session and nothing is left behind;
a path caches durably and each set gets \verb{<file>-<set>}.}

\item{refit}{Passed to \code{\link[=fit_cached]{fit_cached()}}.}

\item{seed}{Passed to the fitter, where it enters the cache key, and
used around the draw subsample so that the same seed keeps the same
\code{n_draws}. \code{NULL} leaves the random number generator alone and is
recorded as \code{NA}.}

\item{...}{Passed to \code{\link[=fit_cached]{fit_cached()}} and on to the fitter: \code{chains},
\code{iter}, \code{backend}, \code{cores}. \code{sample_prior} is set here and giving it
is an error.}

\item{.fitter}{The fitting function, \code{bmm::bmm()} by default. Tests
inject a stand-in so that no model is compiled.}
}
\value{
A \code{bmmtools_prior_check} tibble, one row per prior set,
response and statistic, with the columns \code{prior}, \code{response},
\code{statistic} and \code{value}. It carries the draws themselves as an
attribute, because a summary cannot be plotted as a distribution,
along with \code{brms::prior_summary()} of each fit --- the prior that
actually produced the draws, rather than the one that was asked for.
}
\description{
The question to ask before fitting anything: a prior on a link-scale
parameter is not interpretable, and the same prior expressed as "this
share of prior draws implies chance-level responding" is. Draws come
from \code{bmm(sample_prior = "only")} through \code{\link[=fit_cached]{fit_cached()}}, so the prior
being checked is exactly the prior bmm fits with --- exact for every
prior brms accepts, and it gives predictions on the observable scale
for free.
}
\details{
A population-level slope left without a proper prior is warned about
before fitting: \code{sample_prior = "only"} samples every parameter and a
flat prior has nothing to sample from. It is a warning rather than an
error because bmm's defaults cover the intercepts and the group-level
SDs, and only a slope you added is likely to be missing.

A floor or ceiling rate is meaningful for a \strong{discrete} response --- a
count out of \code{n_trials} --- where it is the share of prior draws
implying perfect or floor performance. For a \strong{continuous} response
exact equality to a boundary has probability zero and the quantiles
are what to read. A circular response error is best read through a
summary of your own on the absolute error, because the quantiles of a
signed error are symmetric about zero and say nothing:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{prior_check(model, formula, data, summary = function(yrep, data) \{
  tibble::tibble(
    statistic = c("q50", "q90"),
    value = unname(stats::quantile(abs(yrep), c(0.5, 0.9)))
  )
\})
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
model <- bmm::mixture2p(resp_error = "y")
checked <- prior_check(
  model, recovery_formula(model), my_data,
  n_draws = 500, seed = 1
)
}

# a precomputed check comparing two prior sets
prior_check_sdt_yn
summary(prior_check_sdt_yn)

}
