% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-ml.R
\name{fit_ml}
\alias{fit_ml}
\title{Subject-wise maximum likelihood estimates of a bmm model}
\usage{
fit_ml(
  model,
  data,
  formula = NULL,
  by = "id",
  method = c("stan", "optim"),
  prior = c("flat", "default"),
  ci_level = 0.95,
  draws = 1000,
  max_abs_link = 20,
  start = NULL,
  nll = NULL,
  file = NULL,
  refit = c("on_change", "never", "always"),
  seed = NULL,
  ...,
  .fitter = NULL
)
}
\arguments{
\item{model}{A \code{bmmodel}, as \code{\link[bmm:mixture2p]{bmm::mixture2p()}} returns.}

\item{data}{The data to fit.}

\item{formula}{An already-reduced \code{bmmformula} with no group-level
terms. \code{NULL}, the default, writes \verb{p ~ 0 + <by>} for every free
parameter. Supply one for a design \code{fit_ml()} does not write itself.
\code{method = "optim"} builds no formula and does not accept one.}

\item{by}{The column defining one independent set of parameters. A
single column in this version.}

\item{method}{\code{"stan"} optimises bmm's own generated likelihood through
\code{algorithm = "laplace"}, re-implementing nothing, and needs CmdStan.
\code{"optim"} maximises bmm's R density for the model directly with
\code{\link[stats:optim]{stats::optim()}} and needs no compiler. See the two-routes section.}

\item{prior}{\code{"flat"}, the default, puts an empty prior on every free
parameter so the mode is the maximum-likelihood estimate.
\code{"default"} keeps bmm's priors, which makes the mode a penalised
estimate rather than an ML one. \code{method = "optim"} has no priors at
all and accepts \code{"flat"} only.}

\item{ci_level}{The interval level. The interval is the Laplace
approximation's on the Stan route (\code{ci_method = "laplace"}) and a
Wald interval from the optimiser's Hessian on the other
(\code{ci_method = "wald"}).}

\item{draws}{How many draws to take from the Laplace approximation.
\code{method = "stan"} only.}

\item{max_abs_link}{The largest \code{abs(estimate)} on the link scale that
still counts as converged.}

\item{start}{Named link-scale start values for \code{method = "optim"}, one
per free parameter. \code{NULL}, the default, starts at zero, which was
measured to reach the same optimum as a start at the Stan mode on
every subject of the reference run.}

\item{nll}{A function of \verb{(pars, data, model)} returning the negative
log-likelihood of one subject's \code{data} as a single number, where
\code{pars} is a named list on the natural scale with the model's fixed
parameters included. Supply it for a model bmmtools carries no
density for. \code{method = "optim"} only.}

\item{file, refit}{Passed to \code{\link[=fit_cached]{fit_cached()}}. \code{NULL} uses a temporary
file, so nothing is cached between sessions. \code{method = "optim"} does
not cache and ignores both: it produces no fit object, and at well
under a second per call a cache would add an invalidation surface
without saving anything.}

\item{seed}{Passed to the fitter. \code{method = "optim"} is deterministic
and ignores it.}

\item{...}{Passed to \code{\link[=fit_cached]{fit_cached()}} and on to the fitter.}

\item{.fitter}{As in \code{\link[=fit_cached]{fit_cached()}}. \code{method = "stan"} only.}
}
\value{
A \code{bmmtools_ml}: a tibble satisfying the estimates contract,
one row per subject and term, with \code{level = "subject"} and
\code{estimator = "ml"}. \code{attr(x, "ml_cells")} holds one row per subject
with its term and convergence counts.
}
\description{
Fits a model with no pooling --- one independent set of parameters per
subject --- and returns the estimates in the same tibble shape a
hierarchical fit gives, so that the two can be scored against one truth
and compared. It exists to measure what hierarchical estimation buys,
not to recommend maximum likelihood for inference.
}
\section{Two routes}{

\code{method = "stan"} optimises bmm's own generated likelihood with
\code{algorithm = "laplace"}. It re-implements nothing, so it works for any
bmm model, and it needs a working CmdStan.

\code{method = "optim"} maximises the likelihood directly with
\code{\link[stats:optim]{stats::optim()}} on bmm's R density for the model. It needs no
compiler and is roughly twenty times faster --- 0.35 s against 8 s for
40 subjects of \code{mixture2p} --- but it is capped at the models bmmtools
carries a density for, listed in the error it raises otherwise, and
\code{nll} is the escape hatch for anything else.

The two routes return the same columns, the same class and the same
\code{estimator}, and differ only in \code{ci_method}. Measured on 40 subjects of
\code{mixture2p} at 100 trials, they agree to Monte Carlo error on the
estimate and their Wald and Laplace standard errors agree to a mean
ratio of 1.000 (range 0.95 to 1.05), giving the same \code{coverage} and
\code{calibration_slope} to three decimals. The route is not a moderator of
a scored number.
}

\section{One fit, not one per subject (\code{method = "stan"})}{

Under \verb{p ~ 0 + <by>} every free parameter belongs to one subject and
bmm's remaining parameters are constants, so the log posterior is a sum
of per-subject terms and the joint mode is the vector of per-subject
modes. The Stan route therefore runs a single fit for the whole data
set. This was checked rather than assumed: over 40 subjects, the joint
Stan mode and independent optimisation of bmm's own density agreed to
3.0e-06 on the link scale.

The cost is that convergence is no longer separable by subject --- one
subject at a boundary can stall the whole optimisation --- so the
per-cell verdict in \code{ml_cells} is the range check described below. The
\code{optim} route does not have this property: it is one independent
optimisation per subject.
}

\section{What counts as converged}{

\code{converged} is \code{TRUE} where the estimate is finite and
\code{abs(estimate) <= max_abs_link} on the link scale, and, on the \code{optim}
route, where \code{optim()} also returned its success code. brms keeps no
optimiser return code on a Laplace fit, so on the Stan route the range
check is the only verdict available --- and not a weak one: fitting a
single subject, Stan reported convergence at a point 0.064
log-likelihood short of the mode.

A row that fails is kept with \code{estimate = NA} rather than dropped,
because \code{metric_bias()} and \code{metric_rmse()} drop incomplete pairs
silently: dropping would compare the Bayesian estimator on every
subject against maximum likelihood on the easiest ones and report the
difference as the estimator's. Report \code{n} alongside any comparison.

On the \code{optim} route a Hessian that cannot be inverted gives a row with
no interval: \code{ci_low} and \code{ci_high} are \code{NA} while the estimate and
\code{converged} stand, so the subject counts in \code{bias}, \code{rmse} and \code{r} and
drops out of \code{coverage} only.

\code{rhat}, \code{ess_bulk} and \code{ess_tail} are always \code{NA}. Neither route
produces MCMC draws, so the values \code{posterior} would return describe
the approximation and not the estimate.
}

\examples{
\dontrun{
model <- bmm::mixture2p(resp_error = "y")
sim <- simulate_recovery(
  model,
  pars = c(kappa = 2, thetat = 1), sds = c(kappa = 0.3, thetat = 0.5),
  n_subjects = 40, n_trials = 100, seed = 1
)
ml <- fit_ml(model, sim$data)
summary(recover_subjects(ml, sim$truth$subjects))

# the same thing without a compiler, and without Stan
ml2 <- fit_ml(model, sim$data, method = "optim")
}

}
\seealso{
\code{\link[=recover_subjects]{recover_subjects()}} to score the result against a known
truth, and \code{\link[=extract_estimates]{extract_estimates()}} for the hierarchical side of the
comparison. \code{bind_rows()} of the two is the join; there is no
separate comparison function. \code{\link[=recovery_grid]{recovery_grid()}} with \code{ml = TRUE}
runs both fits on every cell of a design and does the join itself.
}
