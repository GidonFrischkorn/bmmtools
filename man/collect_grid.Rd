% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect.R
\name{collect_grid}
\alias{collect_grid}
\title{Read a finished recovery grid from its cell files}
\usage{
collect_grid(dir, scale = NULL, levels = NULL, correlations = NULL)
}
\arguments{
\item{dir}{The directory a \code{\link[=recovery_grid]{recovery_grid()}} run wrote, holding
\code{grid.rds} and the cell files. Written by bmmtools 0.2.0 or later;
an earlier run left no \code{grid.rds} and cannot be collected.}

\item{scale}{The scale to score on, as in \code{\link[=recover]{recover()}}. \code{NULL}, the
default, uses the scale the grid was run on. Giving it re-scores the
stored rows, so a grid run on the link scale can be read again on
the natural one without refitting.}

\item{levels, correlations}{What to score, as in \code{\link[=recovery_grid]{recovery_grid()}}.
\code{NULL} uses what the grid requested. Either may be narrowed to a
subset of what the cells hold; asking for something they do not hold
is an error, because a level is extracted from a fit and the fits
are not read here.}
}
\value{
A \code{bmmtools_recovery} with the attributes \code{\link[=recovery_grid]{recovery_grid()}}
gives it. \code{cells$elapsed} is \code{NA}: no cell was run, so none took
any time. \code{cells$fit_seconds} is the time each fit took when it was
run, which is what a runtime table wants and what survives the trip
between machines.
}
\description{
What \code{\link[=recovery_grid]{recovery_grid()}} returns, rebuilt from the files it left in
\code{dir}, without fitting anything and without reading a fit. The
per-cell sidecars hold everything that was scored, so a study can run
on one machine and be assembled on another.
}
\details{
This is a reader, not a resume. It checks neither the cache key nor
the version of bmmtools that wrote the files, because on the machine
that collects the results neither can match: the key covers the
installed bmm, brms and Stan toolchain. A resume is what it has always
been --- rerunning the identical \code{\link[=recovery_grid]{recovery_grid()}} call in the same
directory --- and it is the resume, not this function, that guarantees
the files describe the call.
}
\examples{
\dontrun{
# on the server
recovery_grid(model, grid, pars, dir = "runs/mixture2p", reps = 20)

# on a laptop, with only runs/mixture2p/*-sim.rds and *-est.rds copied
out <- collect_grid("runs/mixture2p")
summary(out)
collect_grid("runs/mixture2p", scale = "link")
}

}
\seealso{
\code{\link[=recovery_grid]{recovery_grid()}}, which writes what this reads.
}
