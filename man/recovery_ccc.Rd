% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{recovery_ccc}
\alias{recovery_ccc}
\title{Concordance between recovered and generating values}
\usage{
recovery_ccc(estimate, truth)
}
\arguments{
\item{estimate, truth}{Numeric vectors of the same length. Incomplete
pairs are dropped.}
}
\value{
A one-row tibble with \code{ccc}, \code{ccc_low}, \code{ccc_high},
\code{ccc_accuracy}, \code{ccc_scale_shift}, \code{ccc_location_shift},
\code{calibration_slope} and \code{n}, the number of complete pairs. The
coefficient and its components are \code{NA} with fewer than three pairs
or when either vector has no spread.
}
\description{
Lin's concordance correlation coefficient with its 95\% interval and
decomposition, for any pair of vectors: subject estimates against
their generating values, or recovered correlations and effects
against the ones that were simulated. \code{summary()} of a recovery
object reports the same columns per parameter.
}
\details{
The coefficient is \code{1 - E[(estimate - truth)^2] / (sd_e^2 + sd_t^2 + (mean_e - mean_t)^2)}, a rescaled mean squared deviation, and factors
into Pearson's \code{r} (precision) times \code{ccc_accuracy} (Lin, 1989, p. 258).
Accuracy depends on the scale shift \code{v = sd_e / sd_t} and the location
shift \verb{u = (mean_e - mean_t) / sqrt(sd_e sd_t)}; all moments divide by
\code{n}.

\strong{Reading the scale shift.} Estimates from a hierarchical model are
shrunk towards the population mean, and a well-calibrated posterior
mean has \code{ccc_scale_shift} equal to \code{r}, not 1. \code{calibration_slope}
(r divided by \code{v}, the slope of truth regressed on the estimate) is 1 in that
case, above 1 when estimates are shrunk too much and below 1 when they
are shrunk too little. Because accuracy treats \code{v} and \code{1/v} alike,
\code{ccc} rewards too little shrinkage: read it together with the RMSE and
the calibration slope, and prefer \code{r} when only the rank order of
subjects matters.

\strong{The interval} uses Lin's Z transformation, \code{atanh(ccc)}, with the
asymptotic variance for random bivariate normal pairs, and is \code{NA}
with three or fewer pairs, at \verb{ccc = +/-1} and when the Pearson
correlation is 0. When the
generating values are fixed by design rather than drawn, it is an
approximation. It is always a 95\% interval.
}
\examples{
truth <- c(0.2, 0.5, 0.9, 1.4, 1.8, 2.3)
# shrunk towards the mean and shifted up
estimate <- 0.7 * truth + 0.5
recovery_ccc(estimate, truth)

}
\references{
Lin, L. I.-K. (1989). A concordance correlation
coefficient to evaluate reproducibility. \emph{Biometrics, 45}(1),
255--268. \doi{10.2307/2532051}

Lin, L. I.-K. (2000). A note on the concordance correlation
coefficient. \emph{Biometrics, 56}(1), 324--325.
\url{https://www.jstor.org/stable/2677159}
}
