% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subject-table.R
\name{subject_table}
\alias{subject_table}
\title{True and estimated subject values, one row per subject}
\usage{
subject_table(
  x,
  fit = NULL,
  point = c("mean", "median"),
  scale = c("link", "natural"),
  links = NULL
)
}
\arguments{
\item{x}{A \code{bmmtools_simulation}, together with \code{fit}; a
\code{bmmtools_simulation_set} from \code{\link[=simulate_components]{simulate_components()}}, together with
its fits; or the result of \code{\link[=recovery_grid]{recovery_grid()}}, whose attribute
\code{subject_means} carries the posterior means, medians and true values of
every cell.}

\item{fit}{The fit of \code{x} when \code{x} is a simulation: a \code{brmsfit}, or any
object with an \code{\link[=extract_subject_draws]{extract_subject_draws()}} method. For a simulation set,
the fits from \code{\link[=fit_components]{fit_components()}}, one per component, whose terms appear
prefixed with the component name. Not used with a grid result.}

\item{point}{The point estimate: the posterior \code{"mean"} or \code{"median"}
of each subject's value.}

\item{scale}{\code{"link"} or \code{"natural"}. On the natural scale the true
values and the point estimates of each parameter go through
\code{\link[=inverse_link]{inverse_link()}}; covariates are left as they are.}

\item{links}{A named character vector mapping a term to a link name.
\code{NULL} reads the simulation model's link table, or the one the grid
stored; without one, the natural scale falls back to the link scale
with a message.}
}
\value{
A tibble with one row per condition, replication and subject:
\code{condition} (\code{NA} for a single simulation), \code{replication} (\code{1} for a
single simulation), \code{id}, then \verb{true_<term>} and \verb{est_<term>} for each
parameter with subject-level draws, then one column per covariate,
under its own name.
}
\description{
Puts every subject's generating value next to its posterior point
estimate, one column pair per parameter, with the covariates alongside.
This is the input for a structural equation model of true against
estimated values, for example in lavaan, which recovers a correlation
that the posterior means attenuate by shrinkage (see
\code{\link[=extract_correlations]{extract_correlations()}}). bmmtools does not fit that model itself and
does not depend on lavaan.
}
\details{
A parameter that does not vary in the simulation has no subject truth;
its \verb{true_<term>} column is the population value for every subject.

On the natural scale the estimate is the inverse link of the point
estimate on the link scale, not the mean of the inverse links, as for
the \code{point} estimator of \code{\link[=extract_correlations]{extract_correlations()}}. For the median the
two are the same.
}
\examples{
\dontrun{
sim <- simulate_recovery(
  bmm::mixture2p(resp_error = "y"),
  pars = c(kappa = log(8), thetat = qlogis(0.75)),
  n_subjects = 100, n_trials = 50,
  sds = c(kappa = 0.3, thetat = 0.5),
  covariates = list(G = c(mean = 0, sd = 1)), seed = 1
)
fit <- bmm::bmm(recovery_formula(sim$model), sim$data, sim$model)
subject_table(sim, fit)

# one table over a grid, from its sidecars
out <- recovery_grid(model, grid, pars, dir = "fits", sds = sds)
subject_table(out, point = "median")
}

}
