% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{summary.bmmtools_cross_check}
\alias{summary.bmmtools_cross_check}
\alias{summary.bmmtools_cross_check_summary}
\title{Summarise a cross-check into per-parameter metrics}
\usage{
\method{summary}{bmmtools_cross_check}(object, ...)

\method{summary}{bmmtools_cross_check_summary}(object, ...)
}
\arguments{
\item{object}{A \code{bmmtools_cross_check} object from \code{\link[=cross_check]{cross_check()}}.}

\item{...}{Not used.}
}
\value{
A \code{bmmtools_cross_check_summary} tibble with the columns
\code{term}, \code{level}, \code{scale}, \code{n}, \code{n_converged}, \code{bias}, \code{rmse},
\code{coverage}, \code{share_overlap}, \code{r}, \code{r_low}, \code{r_high}, \code{ccc},
\code{ccc_low} and \code{ccc_high}.
}
\description{
One row per term and level: the mean and root mean square of
\code{estimate - reference}, the share of fit intervals covering the
reference, the share of rows whose intervals overlap, and the
correlation and Lin's concordance between the two sides.
}
\details{
\code{bias} here is a difference from a comparison value, not from a known
truth; see \code{\link[=cross_check]{cross_check()}}.

\code{r} and \code{ccc} come from the pairs of \code{estimate} and \code{reference} in the
group and are \code{NA}, never \code{0}, below three complete pairs or without
spread on either side. At the population level with one fit each term
contributes a single pair, so both are \code{NA} and the table is the
per-term difference and coverage; at the subject level they are the
comparison across subjects. \code{share_overlap} is \code{NA} for a reference
without intervals. \code{n_converged} counts the rows whose fit passed
\code{\link[=check_convergence]{check_convergence()}} and is \code{NA} when no row carries a verdict.
}
