% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimates.R
\name{extract_estimates}
\alias{extract_estimates}
\alias{extract_estimates.default}
\alias{extract_estimates.brmsfit}
\title{Extract a tidy table of parameter estimates from a fit}
\usage{
extract_estimates(fit, ...)

\method{extract_estimates}{default}(fit, ...)

\method{extract_estimates}{brmsfit}(
  fit,
  level = c("population", "subject", "sd", "cor"),
  group = NULL,
  ci_level = 0.95,
  ci_method = "eti",
  drop_constants = TRUE,
  converged = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{brmsfit}, and so also a \code{bmmfit}.}

\item{...}{Not used. Present so the generic can gain arguments later;
anything passed is an error.}

\item{level}{Which estimates to return: \code{"population"}, \code{"subject"},
\code{"sd"} (the group-level standard deviations), \code{"cor"} (the
group-level correlations), or several, in which case they are
stacked and the \code{level} column separates them.}

\item{group}{The grouping factor subject-level, SD and correlation
estimates come from. \code{NULL} uses the fit's only grouping factor and
errors if there is more than one.}

\item{ci_level}{The interval mass, a single number strictly between 0
and 1.}

\item{ci_method}{The interval type. Only \code{"eti"}, the equal-tailed
interval, is available in this version; the column is carried so
that adding others later is not a breaking change.}

\item{drop_constants}{Drop the parameters the model fixed to
constants. A constant is identified by zero posterior variance, not
by a missing rhat, so that a chain that broke is never silently
dropped as though it had been fixed on purpose.}

\item{converged}{Whether the fit passed the convergence gate. \code{NULL},
the default, computes it as \code{check_convergence(fit)$pass} with the
default thresholds; a logical scalar is used as given, so a verdict
from \code{\link[=check_convergence]{check_convergence()}} with other thresholds can be passed in;
\code{NA} marks it unknown.}
}
\value{
A tibble with the columns \code{term}, \code{estimate}, \code{ci_low},
\code{ci_high}, \code{ci_method}, \code{ci_level}, \code{rhat}, \code{ess_bulk}, \code{ess_tail},
\code{level}, \code{id} and \code{converged}, in that order. \code{id} is \code{NA} except on
subject rows; \code{converged} is the same value on every row of a fit.
}
\description{
The bridge between a fitted model and everything bmmtools scores. It
returns the \emph{estimates tibble}: one row per parameter, with the
posterior median, a credible interval and the convergence diagnostics,
under bmm's own parameter names. \code{\link[=recover]{recover()}} and
\code{\link[=recover_subjects]{recover_subjects()}} take either a fit or one of these tibbles, so a
scoring pipeline can be built, tested and run with no fitting package
installed.
}
\details{
The first nine columns are the apabayes \code{parameters} contract, in its
order, so \code{apabayes::apabayes_tidy(x, type = "parameters")} accepts
the result without renaming.

Subject-level estimates are the \strong{per-draw sum} of the population
intercept and the group-level deviation, summarised afterwards. That
puts them on the same scale as the population rows, so a per-subject
truth can be compared against them directly; taking the sum after
summarising instead would give intervals that are too narrow.

Standard deviations and correlations are returned on the link scale,
with \code{id} set to \code{NA}. An SD row carries the parameter's name
(\code{kappa}); a correlation row carries the two names joined by \verb{__} and
sorted in the C locale (\code{kappa__thetat}), whichever order brms used.
A correlation the model does not estimate has no row. A parameter
with more than one group-level coefficient is an error in this
version.
}
\examples{
\dontrun{
fit <- bmm::bmm(bmm::bmf(kappa ~ 1, thetat ~ 1), data, model)
extract_estimates(fit)
extract_estimates(fit, level = "subject", ci_level = 0.89)
}

}
