% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross-check.R
\name{cross_check}
\alias{cross_check}
\title{Compare a fit's estimates with a reference}
\usage{
cross_check(
  fit,
  reference,
  scale = c("natural", "link"),
  links = NULL,
  level = c("population", "subject"),
  group = "id",
  ci_level = 0.95,
  ...
)
}
\arguments{
\item{fit}{A fit with an \code{\link[=extract_estimates]{extract_estimates()}} method, so a \code{brmsfit}
and therefore a \code{bmmfit}. One fit: a list of them is an error
pointing at \code{\link[=recover]{recover()}}, which scores replications against one truth
and separates them with a \code{replication} column.}

\item{reference}{The right-hand side. A data frame with \code{term} and
\code{estimate}, optionally \code{ci_low} and \code{ci_high} (both or neither),
\code{source} and, at the subject level, \code{id}; its values are read on
\code{scale}. Or another fit, which is extracted at the same level,
transformed the same way, and marked \code{source = "fit"}.}

\item{scale}{\code{"natural"} compares on the scale a reader interprets,
\code{"link"} on the scale the model was estimated on. The fit's
estimates and interval bounds go through \code{\link[=inverse_link]{inverse_link()}} exactly as
\code{\link[=recover]{recover()}} transforms them, so a natural-scale \code{cross_check()} and a
natural-scale \code{recover()} on the same fit agree to the last digit. A
data-frame \code{reference} is taken to be on \code{scale} already and is not
transformed.}

\item{links}{A named character vector mapping a term to one of the
link names \code{\link[=inverse_link]{inverse_link()}} understands. \code{NULL} reads the link table
from a \code{bmmfit}; with no table available the comparison falls back to
the link scale and says so.}

\item{level}{\code{"population"} or \code{"subject"}. \code{"subject"} needs an \code{id}
on both sides. \code{"sd"} and \code{"cor"} are not compared in this version:
a correlation reference would need \code{var1}, \code{var2} and a
pair-ordering rule of its own.}

\item{group}{The grouping variable subject-level estimates come from,
passed to \code{\link[=extract_estimates]{extract_estimates()}}.}

\item{ci_level}{The interval mass, passed to \code{\link[=extract_estimates]{extract_estimates()}}.}

\item{...}{Passed to \code{\link[=extract_estimates]{extract_estimates()}} for both sides: \code{converged},
\code{ci_method}.}
}
\value{
A \code{bmmtools_cross_check} object: a tibble subclass with the
columns \code{term}, \code{estimate}, \code{ci_low}, \code{ci_high}, \code{ci_method},
\code{ci_level}, \code{rhat}, \code{ess_bulk}, \code{ess_tail}, \code{reference}, \code{ref_low},
\code{ref_high}, \code{source}, \code{bias}, \code{covered}, \code{overlap}, \code{scale},
\code{level}, \code{id} and \code{converged}. \code{ref_low}, \code{ref_high} and \code{overlap}
are \code{NA} for a reference without intervals. Call \code{\link[=summary]{summary()}} on it
for the per-parameter metrics and \code{\link[=plot_recovery]{plot_recovery()}} for the picture.
}
\description{
The question a model answers last: does it agree with what is already
known? \code{cross_check()} puts a fit's estimates beside a \strong{reference}
--- a closed form such as \code{bmm::sdt_d()}, another implementation, or
published values --- and returns the comparison in the tibble shape
every other bmmtools scorer returns.
}
\details{
\strong{The reference is a comparison, not a truth.} \code{bias} is the signed
difference from it and \code{covered} says the fit's interval contains it;
neither asserts that the reference is correct. Where the two disagree,
the reference is as much a candidate for the error as the fit is, and
a reference derived under assumptions the fit does not make (an
equal-variance d′ against an unequal-variance fit, say) will disagree
for reasons that are nobody's bug.

A reference term the fit did not estimate is a warning listing what
the fit has, and is dropped; a fit term the reference does not mention
is dropped in silence, because a fit routinely estimates more than a
reference covers. No term matching at all is an error rather than an
empty result. At the subject level the same holds for ids.

\strong{d′ and d_a.} \code{bmm::sdt_d()} returns the equal-variance d′. bmm's
\code{sdt_yn} model estimates d_a = √2·δ/√(1 + r²), which equals d′ only
when \code{sdratio} is fixed at 0. Against a fit with a free \code{sdratio} the
reference is converted by the user, \verb{d_a = d' * sqrt(2 / (1 + exp(sdratio)^2))}; \code{cross_check()} compares what it is given and does
not guess a conversion, because the right one depends on which
\code{sdratio} the fit used.
}
\examples{
\dontrun{
# a signal-detection fit against the closed form, equal-variance case
fit <- bmm::bmm(
  bmm::bmf(d ~ 1, criterion ~ 1),
  data, bmm::sdt_yn(response = "resp", stimulus = "stim", n_trials = "n")
)
reference <- tibble::tibble(
  term = c("d", "criterion"),
  estimate = c(
    bmm::sdt_d(hit_rate = 0.8, fa_rate = 0.2),
    bmm::sdt_criterion(hit_rate = 0.8, fa_rate = 0.2)
  ),
  source = "closed form"
)
cross_check(fit, reference)

# with sdratio free the fit estimates d_a, so the reference converts
sdratio <- 0.2
reference$estimate[1] <- reference$estimate[1] *
  sqrt(2 / (1 + exp(sdratio)^2))
cross_check(fit, reference)
}

}
