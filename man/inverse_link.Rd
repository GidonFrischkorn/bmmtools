% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/links.R
\name{inverse_link}
\alias{inverse_link}
\title{Transform values from the link scale to the natural scale}
\usage{
inverse_link(x, link)
}
\arguments{
\item{x}{A numeric vector of values on the link scale. \code{NA} is passed
through at its own position.}

\item{link}{A single string naming the link: one of \code{"identity"},
\code{"log"}, \code{"softplus"}, \code{"log1p"}, \code{"logm1"}, \code{"inverse"}, \code{"sqrt"},
\code{"logit"}, \code{"probit"}, \code{"tan_half"}, \code{"loglog"} or \code{"cloglog"}.
\code{NULL} is a synonym for \code{"identity"}.}
}
\value{
A double vector the same length as \code{x}, on the natural scale.
}
\description{
Cognitive measurement models are estimated on a link scale that keeps
parameters unconstrained --- a memory precision is sampled as
\code{log(kappa)}, a mixture weight as \code{logit(theta)} --- while readers
interpret them on the natural scale. \code{inverse_link()} is the
back-transform, applied by \code{\link[=recover]{recover()}} to estimates, interval bounds
and generating values alike so that both sides of a recovery
comparison are on the same scale.
}
\details{
The vocabulary and the closed forms are taken from bmm's internal
\code{link_transform()} (\code{bmm/R/helpers-parameters.R}), so the two cannot
disagree about what a link name means. bmmtools ships its own copy
because \code{link_transform()} is not exported.

Values outside a link's natural domain are transformed as R computes
them rather than being replaced by \code{NA}: \code{inverse_link(0, "inverse")}
is \code{Inf} and \code{inverse_link(-2, "sqrt")} is \code{4}. In a scoring pipeline
a visible \code{Inf} is safer than a silent \code{NA}, which would be absorbed
by a metric's missing-data guard and reported as a smaller sample
rather than as a problem.
}
\examples{
inverse_link(c(-1, 0, 1), "log")
inverse_link(0, "logit")

# a precision sampled on the log scale, back on its own scale
inverse_link(c(1.6, 2.3), "log")

}
