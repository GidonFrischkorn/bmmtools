% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convergence.R
\name{check_convergence}
\alias{check_convergence}
\alias{check_convergence.default}
\alias{check_convergence.brmsfit}
\title{Check whether a fit passes the convergence gate}
\usage{
check_convergence(fit, ...)

\method{check_convergence}{default}(fit, ...)

\method{check_convergence}{brmsfit}(
  fit,
  rhat_max = 1.05,
  ess_bulk_min = 400,
  ess_tail_min = NULL,
  divergent_max = 10,
  treedepth_max = NULL,
  variables = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{brmsfit}, and so also a \code{bmmfit}.}

\item{...}{Passed to methods.}

\item{rhat_max}{Largest acceptable rhat over the variables considered.}

\item{ess_bulk_min}{Smallest acceptable bulk effective sample size.}

\item{ess_tail_min}{Smallest acceptable tail effective sample size.
\code{NULL}, the default, reports it without gating on it.}

\item{divergent_max}{Largest acceptable number of divergent
transitions after warmup, summed over chains.}

\item{treedepth_max}{The sampler's maximum tree depth, used to count
the post-warmup iterations that hit it. \code{NULL} reads it from the fit
and falls back to Stan's default of 10. Tree-depth hits are reported
and never gated on: hitting the maximum is an efficiency warning,
not a validity failure.}

\item{variables}{Names of the draws variables to consider. \code{NULL}
means every variable \code{posterior::summarise_draws()} returns.
Variables with a missing rhat, which is what a parameter fixed to a
constant has, are dropped rather than failed on.}
}
\value{
A one-row tibble with \code{max_rhat}, \code{min_ess_bulk},
\code{min_ess_tail}, \code{n_divergent}, \code{n_max_treedepth}, \code{n_variables},
\code{pass} and \code{failed}, the last a comma-separated list of the
criteria that failed (\code{""} when none did). \code{pass} is \code{NA}, not
\code{TRUE}, when no variable had a finite rhat. The thresholds used are
stored in the attribute \code{thresholds}.
}
\description{
One row of worst-case diagnostics and a verdict. The thresholds are
arguments with the defaults the validation scripts in bmm used: rhat
at most 1.05, bulk ESS at least 400, at most ten divergent
transitions. A fit that fails is meant to be \strong{scored but flagged},
never dropped: \code{\link[=extract_estimates]{extract_estimates()}} carries the verdict as its
\code{converged} column and \code{summary()} of a recovery object counts it as
\code{n_converged}.
}
\details{
The numbers come from \code{posterior::summarise_draws()} and
\code{brms::nuts_params()}. A fit without sampler diagnostics (a
variational fit, or one whose sampler parameters were not saved)
leaves \code{n_divergent} and \code{n_max_treedepth} as \code{NA} and does not fail
the divergence criterion, so a reader can see it was not checked.
}
\examples{
\dontrun{
fit <- bmm::bmm(bmm::bmf(kappa ~ 1, thetat ~ 1), data, model)
check_convergence(fit)
check_convergence(fit, rhat_max = 1.01, ess_tail_min = 400)
}

}
