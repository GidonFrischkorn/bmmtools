% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{fit_components}
\alias{fit_components}
\title{Fit every component of a simulation set, each on its own}
\usage{
fit_components(sim_set, dir, prior = NULL, ..., .fitter = NULL)
}
\arguments{
\item{sim_set}{A \code{bmmtools_simulation_set} from \code{\link[=simulate_components]{simulate_components()}}.}

\item{dir}{The directory the fits are saved in, one file per component,
named after it (\verb{<dir>/<name>.rds}, with its cache key next to it).}

\item{prior}{\code{NULL}, or a named list with a prior for some of the
components, such as \verb{list(sdm = <brmsprior>)}. A component not named is
fitted with bmm's default priors.}

\item{...}{Passed to \code{\link[=fit_cached]{fit_cached()}} and on to the fitter, for every
component: \code{refit}, and sampler arguments such as \code{chains}, \code{iter} or
\code{backend}. When the set was simulated with a seed and \code{...} has no
\code{seed}, the set's seed is passed as \code{seed}, the same for every component.}

\item{.fitter}{The fitting function, \code{bmm::bmm()} by default; see
\code{\link[=fit_cached]{fit_cached()}}.}
}
\value{
A named list of fits, one per component, of class
\code{bmmtools_fit_set}, which \code{\link[=extract_correlations]{extract_correlations()}},
\code{\link[=recover_correlations]{recover_correlations()}} and \code{\link[=subject_table]{subject_table()}} read as one set.
}
\description{
Fits each component's data with its own model through \code{\link[=fit_cached]{fit_cached()}}, so
a second call reuses the saved fits while nothing that determines them
has changed.
}
\details{
A component fitted without a \code{formula} uses \code{\link[=recovery_formula]{recovery_formula()}}, with
the task column when the component has tasks.
}
\examples{
\dontrun{
fits <- fit_components(set, dir = "fits", chains = 4, iter = 2000)
fits
}

}
