% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlations.R
\name{extract_correlations}
\alias{extract_correlations}
\title{Extract between-subject correlations from a fit}
\usage{
extract_correlations(
  fit,
  estimator = c("model", "draws", "point"),
  pairs = NULL,
  covariates = NULL,
  group = NULL,
  scale = c("link", "natural"),
  links = NULL,
  ci_level = 0.95,
  converged = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{brmsfit} (so also a \code{bmmfit}), or any object with an
\code{\link[=extract_subject_draws]{extract_subject_draws()}} method.}

\item{estimator}{One or more of \code{"model"}, \code{"draws"} and \code{"point"}. All
three by default.}

\item{pairs}{\code{NULL} for every pair of subject terms and covariates, or
a character vector of pair terms such as \code{"kappa__thetat"}, in either
order.}

\item{covariates}{\code{NULL}; a character vector of columns of \code{fit$data}
that are constant within subject; or a data frame with the grouping
column and one numeric column per covariate, one or several rows per
subject.}

\item{group}{The grouping factor. \code{NULL} uses the fit's only one.}

\item{scale}{\code{"link"} or \code{"natural"}. On the natural scale each term is
back-transformed with \code{\link[=inverse_link]{inverse_link()}} before correlating.}

\item{links}{A named character vector mapping a term to a link name.
\code{NULL} reads the link table of a \code{bmmfit}; without one, the natural
scale falls back to the link scale with a message.}

\item{ci_level}{The interval mass, a single number strictly between 0
and 1.}

\item{converged}{Whether the fit passed the convergence gate. \code{NULL}
computes it as \code{\link[=extract_estimates]{extract_estimates()}} does for a \code{brmsfit} and leaves
it \code{NA} for other classes; a logical scalar is used as given.}

\item{...}{Not used.}
}
\value{
A tibble with the columns \code{term}, \code{var1}, \code{var2}, \code{estimator},
\code{estimate}, \code{ci_low}, \code{ci_high}, \code{ci_method}, \code{ci_level}, \code{rhat},
\code{ess_bulk}, \code{ess_tail}, \code{scale}, \code{n} (subjects) and \code{converged}.
\code{term} is \code{var1__var2}, the two names sorted in the C locale.
}
\description{
Three estimates of how two subject-level parameters, or a parameter
and an observed covariate, correlate across subjects:
}
\details{
\itemize{
\item \code{"model"}: the group-level correlation the model estimates, as in
\code{extract_estimates(level = "cor")}. It exists only for parameters
whose random effects share a correlation block (\code{(1 | p | id)}).
\item \code{"draws"}: in each posterior draw, the Pearson correlation of the
subject values across subjects; summarised like any other posterior
(median, equal-tailed interval, rhat, ESS).
\item \code{"point"}: the Pearson correlation of the subjects' posterior means,
with a Fisher-z interval over the subjects.
}

They answer different questions and do not agree. When the model
cannot separate two parameters within a subject, their posterior means
inherit that trade-off and \code{point} is biased even where the true
correlation is 0; \code{draws} is not. \code{point} is also attenuated by
shrinkage. See \code{\link[=recover_correlations]{recover_correlations()}} for scoring them against the
truth.

\strong{The model estimator} is on the link scale only. Under
\code{scale = "natural"} its rows are left out and a message says so. A
pair involving a covariate never has a model row, and neither does a
pair the model does not correlate.

\strong{The natural scale} for \code{point} takes the inverse link of each
subject's posterior mean on the link scale, not the mean of the inverse
links. Covariates are never transformed.

\strong{Intervals.} \code{ci_method} is \code{"eti"} for \code{model} and \code{draws} and
\code{"fisher_z"} for \code{point}, whose interval treats the posterior means as
if they were observed values; its \code{rhat} and ESS are \code{NA}.

\strong{Missing values.} A correlation is \code{NA}, never 0, with fewer than
three subjects or when one side has no spread across subjects. For
\code{draws} that is decided per draw, and the summary uses the draws that
remain.
}
\examples{
\dontrun{
fit <- bmm::bmm(recovery_formula(model, re_cor = "all"), data, model)
extract_correlations(fit)
extract_correlations(fit, estimator = "draws", covariates = "G")
}

}
