% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_recovery}
\alias{plot_recovery}
\alias{plot_recovery.default}
\alias{plot_recovery.bmmtools_recovery}
\alias{plot_recovery.bmmtools_cor_recovery}
\alias{plot_recovery.bmmtools_cross_check}
\title{Plot recovered estimates against their generating values}
\usage{
plot_recovery(x, ...)

\method{plot_recovery}{default}(x, ...)

\method{plot_recovery}{bmmtools_recovery}(
  x,
  facet_by = "term",
  color_by = NULL,
  intervals = TRUE,
  identity_line = TRUE,
  scales = "free",
  annotate = FALSE,
  ...
)

\method{plot_recovery}{bmmtools_cor_recovery}(
  x,
  truth = c("sample", "true"),
  facet_by = "term",
  color_by = "estimator",
  intervals = TRUE,
  identity_line = TRUE,
  ...
)

\method{plot_recovery}{bmmtools_cross_check}(
  x,
  facet_by = "term",
  color_by = NULL,
  intervals = TRUE,
  identity_line = TRUE,
  scales = "free",
  ...
)
}
\arguments{
\item{x}{A \code{bmmtools_recovery} object from \code{\link[=recover]{recover()}} or
\code{\link[=recover_subjects]{recover_subjects()}}, a \code{bmmtools_cor_recovery} object from
\code{\link[=recover_correlations]{recover_correlations()}}, or a \code{bmmtools_cross_check} object from
\code{\link[=cross_check]{cross_check()}}.}

\item{...}{Not used.}

\item{facet_by}{A column name to make panels from, or \code{NULL} for a
single panel. Defaults to \code{"term"}: parameters usually live on
scales too different to share an axis.}

\item{color_by}{A column name to colour points by, or \code{NULL}. \code{NULL}
by default for a recovery object and \code{"estimator"} for a correlation
recovery.}

\item{intervals}{Draw the intervals as bars.}

\item{identity_line}{Draw the line where the estimate equals the
generating value.}

\item{scales}{Passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}. \code{"free"} by default,
for the same reason \code{facet_by} is.}

\item{annotate}{Label each panel with the Pearson correlation and
Lin's concordance from \link[=summary.bmmtools_recovery]{summary()} of
the rows in that panel. Each panel must then hold a single term,
level and condition; filter the object first if it does not.}

\item{truth}{For a correlation recovery, the value on the x axis:
\code{"sample"}, the in-sample correlation, or \code{"true"}, the generating
one. Rows without that value (a nonzero \code{true_value} on the natural
scale is \code{NA}) are left out with a message.}
}
\value{
A \code{ggplot} object.
}
\description{
The picture a recovery table is read through: the generating value on
the x axis, the posterior median on the y axis, the credible interval
as a bar and an identity line to compare against. A model that
recovers its parameters puts the points on the line; a model that is
biased puts them on a line beside it, and a model that cannot
distinguish them puts them on a cloud.
}
\details{
For a correlation recovery from \code{\link[=recover_correlations]{recover_correlations()}} the x axis is
the in-sample correlation of the simulated subjects (\code{truth = "sample"}) or the generating correlation (\code{truth = "true"}), and the
points are coloured by estimator.

For a cross-check from \code{\link[=cross_check]{cross_check()}} the x axis is the reference,
the fit's interval runs vertically and the reference's own interval,
where it has one, runs horizontally. Panels default to one per term at
the subject level and to a single panel at the population level, where
each term contributes one point; \code{facet_by} overrides either.
}
\examples{
\dontshow{if (rlang::is_installed("ggplot2")) withAutoprint(\{ # examplesIf}
# subject-level recovery in one cell of the example grid
cell <- dplyr::filter(
  recovery_mixture2p,
  level == "subject", condition == "row-4"
)
plot_recovery(cell)
plot_recovery(cell, annotate = TRUE)

# population-level estimates, coloured by design cell
population <- dplyr::filter(recovery_mixture2p, level == "population")
plot_recovery(population, color_by = "condition")
\dontshow{\}) # examplesIf}
}
