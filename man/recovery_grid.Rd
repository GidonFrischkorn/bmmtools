% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{recovery_grid}
\alias{recovery_grid}
\title{Run a parameter-recovery grid}
\usage{
recovery_grid(
  model,
  grid,
  pars,
  dir,
  reps = 1,
  sds = NULL,
  cors = NULL,
  covariates = NULL,
  tasks = NULL,
  task_col = "task",
  coding = c("cell", "contrast"),
  contrasts = NULL,
  formula = NULL,
  prior = NULL,
  generator = NULL,
  seed = NULL,
  subjects = c("redraw", "fixed"),
  re_cor = c("none", "within", "all"),
  scale = c("natural", "link"),
  levels = c("population", "subject"),
  correlations = NULL,
  cor_scale = "link",
  convergence = NULL,
  ml = FALSE,
  smoke = FALSE,
  preflight = TRUE,
  ...,
  .fitter = NULL
)
}
\arguments{
\item{model}{A \code{bmmodel}, or a function of one grid row (a one-row
data frame) returning one, for models whose constructor depends on
the design. Or a list of \code{\link[=recovery_component]{recovery_component()}}s, or a function of the
row returning such a list, to simulate several models for the same
people and fit each on its own; see the section "Components".}

\item{grid}{A data frame with the columns \code{n_subjects} and \code{n_trials}
(with components, \code{n_subjects} only; see "Components" for their
columns).
A column named after a parameter gives that cell's population value
on the link scale, overriding \code{pars}; a column \verb{sd_<parameter>}
overrides \code{sds}; a column \verb{cor_<a>__<b>} sets the correlation of two
parameters or covariates (the names in either order). With \code{tasks},
these columns may also use full task terms (\code{kappa_task2},
\code{sd_kappa_task2}, \code{cor_kappa_task1__kappa_task2}); a bare parameter
column sets every task, and a full-term column in the same row
overrides it for its task. A SimDesign design is a data frame and
works as is.}

\item{pars, sds, cors}{Defaults for every cell, as in
\code{\link[=simulate_recovery]{simulate_recovery()}}. Each may also be a \verb{function(row)} of the
one-row grid data frame, evaluated under the cell's seed, which draws
new hyperparameters for every data set; the grid columns above are
applied to its result.}

\item{dir}{Directory for the per-cell files; created if missing.}

\item{reps}{Replications per cell.}

\item{covariates}{As in \code{\link[=simulate_recovery]{simulate_recovery()}}, the same for every cell.}

\item{tasks, task_col}{As in \code{\link[=simulate_recovery]{simulate_recovery()}}, the same for every
cell. With \code{tasks}, the default formula is
\code{recovery_formula(model, re_cor = re_cor, task_col = task_col)}.}

\item{coding, contrasts}{As in \code{\link[=simulate_recovery]{simulate_recovery()}}, the same for every
cell. \code{coding = "contrast"} needs \code{tasks}, makes the default formula
\code{1 + task} rather than \code{0 + task}, and scores the intercept and the
contrasts in place of the cell means. What is simulated does not
change, so a grid can be run under both codings from one set of
generating values.}

\item{formula}{A \code{bmmformula}; \code{NULL} means \code{\link[=recovery_formula]{recovery_formula()}} of the
cell's model; or a \verb{function(row)} of the one-row grid data frame
returning a \code{bmmformula}, called once per row, for designs whose
formula depends on the row.}

\item{prior}{Passed to \code{\link[=fit_cached]{fit_cached()}}; with components, \code{NULL} or a list
with a prior per component.}

\item{generator}{As in \code{\link[=simulate_recovery]{simulate_recovery()}}.}

\item{seed}{Master seed. Each cell derives its own from it and the
cell's row and replication, so a cell is reproducible on its own.
\code{NULL} leaves everything unseeded and records \code{NA}.}

\item{subjects}{\code{"redraw"} draws new subject values in every
replication; \code{"fixed"} draws them once per row and reuses them, so
replications become a simulated retest of the same people. The
realised population values, SDs, correlations and covariate values of
replication 1 are reused too.}

\item{re_cor}{Passed to \code{\link[=recovery_formula]{recovery_formula()}} when \code{formula} is \code{NULL}.}

\item{scale}{The scale recovery is scored on, as in \code{\link[=recover]{recover()}};
natural by default.}

\item{levels}{The estimate levels extracted from every fit and scored:
one or more of \code{"population"}, \code{"subject"} and \code{"sd"} (the
between-subject standard deviations, always scored on the link
scale).}

\item{correlations}{\code{NULL} (the default) for no correlations, or one or
more of the estimators of \code{\link[=extract_correlations]{extract_correlations()}}, \code{"model"},
\code{"draws"} and \code{"point"}, to extract and score the between-subject
correlations of every cell, pairs with covariates included.}

\item{cor_scale}{The scale the correlations are extracted and scored
on, \code{"link"} or \code{"natural"}. The \code{"model"} estimator exists on the
link scale only.}

\item{convergence}{\code{NULL}, the default, gates every cell with
\code{\link[=check_convergence]{check_convergence()}}'s defaults. A named list gives its arguments
(\code{rhat_max}, \code{ess_bulk_min}, \code{ess_tail_min}, \code{divergent_max},
\code{treedepth_max}, \code{variables}), checked before any cell runs. The
gate is computed once per cell and its whole diagnostics row is
stored in the cell's sidecar, so a study can report why a cell
failed without reading the fit.}

\item{ml}{\code{FALSE}, the default, fits every cell hierarchically only.
\code{TRUE} also fits every cell subject by subject with \code{\link[=fit_ml]{fit_ml()}} on
the same simulated data, and scores both estimators against one
truth at the subject level; the ML rows carry \code{estimator = "ml"}. A
named list gives arguments to \code{\link[=fit_ml]{fit_ml()}} (\code{prior}, \code{draws},
\code{max_abs_link}, \code{ci_level}, \code{formula}, \code{.fitter}, and through its
\code{...} the fitter's own, such as \verb{refresh = 0, silent = 2} to quiet
the optimiser); \code{model}, \code{data}, \code{file}, \code{refit}, \code{seed} and \code{by}
are the grid's. Needs \code{"subject"} in \code{levels}. See the section
"Subject-wise ML".}

\item{smoke}{\code{TRUE} runs the first two rows with two replications
into \verb{<dir>/smoke}, so a smoke run never overwrites a full one.}

\item{preflight}{Run the first cell once with one chain and 200
iterations into \verb{<dir>/preflight} before the loop. Skipped when the
first cell already has a cached fit or a sidecar that can be used.}

\item{...}{Passed to \code{\link[=fit_cached]{fit_cached()}} and so to the fitter: \code{chains},
\code{iter}, \code{warmup}, \code{backend}, \code{cores}, ...}

\item{.fitter}{As in \code{\link[=fit_cached]{fit_cached()}}.}
}
\value{
A \code{bmmtools_recovery} object over every cell that produced a
fit, at the requested \code{levels}, with a \code{condition} column
(\code{"row-<i>"}) naming the grid row. \code{summary()} groups by condition.
Its attributes:
\itemize{
\item \code{cells}: a tibble with one row per cell (\code{condition},
\code{replication}, \code{n_subjects}, \code{n_trials}, \code{seed}, \code{file}, \code{status},
\code{elapsed} in seconds, \code{converged}), followed by the cell's
convergence record (\code{max_rhat}, \code{min_ess_bulk}, \code{min_ess_tail},
\code{n_divergent}, \code{n_max_treedepth}, \code{n_variables}, \code{failed}) and
what it was run with (\code{fit_seconds}, \code{chains}, \code{iter},
\code{threads}). All of these come from the cell's sidecar, so a
resumed grid reports them without reading a fit. \code{elapsed} is how
long the cell took in this run, read time included;
\code{fit_seconds} is how long the fit itself took when it was run.
\item \code{grid}: the grid as run. The same record, with everything else the
grid was called with, is written to \verb{<dir>/grid.rds}, which is
what \code{\link[=collect_grid]{collect_grid()}} reads.
\item \code{correlations}: with \code{correlations} requested, a
\code{bmmtools_cor_recovery} from \code{\link[=recover_correlations]{recover_correlations()}} over every
cell, with \code{condition} and \code{replication}; otherwise \code{NULL}.
\item \code{subject_means}: a tibble with one row per cell, subject and term:
\code{condition}, \code{replication}, \code{id}, \code{term}, \code{covariate} (whether the
term is a covariate), the posterior \code{mean} and \code{median} on the link
scale, and \code{true_value}, the generating value on the link scale (a
covariate's value is its own mean, median and true value). Its
attribute \code{links} is the model's link table. \code{\link[=subject_table]{subject_table()}}
turns it into one row per subject.
\item \code{ml_cells}: with \code{ml}, a tibble with one row per cell (\code{condition},
\code{replication}, \code{status}, \code{elapsed}, \code{n_subjects}, \code{n_converged},
\code{converged}, \code{file}) for the ML fit; see "Subject-wise ML".
\code{cells} stays one row per cell and describes the hierarchical fit.
}
}
\description{
The loop the validation scripts in bmm wrote by hand, with the
requirements their overnight runs taught: one durable
file per cell written as soon as the cell finishes, a resume that
reads those files and needs no temporary state, a smoke mode in its
own directory, a preflight fit that catches a compile or init error
before any cell runs, and cells ordered so that the first completed
block spans the design rather than exhausting one level.
}
\details{
A cell whose fit errors is recorded with \code{status = "error"} and the
grid goes on; a warning at the end names the failed cells. A cell
whose data cannot be generated stops the grid, because that is a
design error rather than a sampler accident. Cell files are
\verb{cell-<row>-rep-<rep>-sim.rds} (data and truth) and
\verb{cell-<row>-rep-<rep>.rds} with its \code{.key} (the fit, through
\code{\link[=fit_cached]{fit_cached()}}); a resume reads an existing simulation file rather
than regenerating, so a cached fit and its truth always match.

\strong{The sidecar.} While a fit is in memory, everything scored from it is
written to \verb{cell-<row>-rep-<rep>-est.rds}: the estimates at \code{levels},
the correlations, the subject means, and what they were extracted
with (the cell's cache key, the bmmtools version, \code{levels},
\code{correlations}, \code{cor_scale}). A resume uses the sidecar without
reading the fit when the key and the version match and it holds every
requested level and estimator, so the fit files may be deleted once a
grid has run. Otherwise the cell goes through \code{\link[=fit_cached]{fit_cached()}} again,
which reuses a cached fit, and the sidecar is rewritten.
}
\section{Subject-wise ML}{

With \code{ml}, every cell is fitted twice on the same simulated data: the
hierarchical fit, and one \code{\link[=fit_ml]{fit_ml()}} call with no pooling, cached as
\verb{cell-<row>-rep-<rep>-ml.rds} under the cell's seed. The ML rows are
bound to the hierarchical subject rows before scoring, so the result
has both estimators at the subject level, \code{"bayes"} and \code{"ml"},
against one truth; \code{summary()} reports them as separate rows and
\code{plot_recovery(color_by = "estimator")} colours them. An ML fit has no
population or \code{sd} level, so those levels stay hierarchical only, and
its subject means are not added to \code{subject_means}. The \code{...} of the
grid (\code{chains}, \code{iter}, \code{backend}, \code{refresh}, ...) belong to the
hierarchical fit and do not reach \code{\link[=fit_ml]{fit_ml()}}; \code{ml = list(draws = , refresh = 0)} and the other \code{\link[=fit_ml]{fit_ml()}} arguments do.

The sidecar records the ML request (its prior, draws, interval level,
link-scale range and formula) with the ML rows, and a resume reuses
them only when the request is the same; a change to \code{max_abs_link} or
\code{ci_level} re-extracts from the cached ML fit without refitting, and a
change to \code{prior}, \code{draws} or \code{formula} refits through \code{\link[=fit_cached]{fit_cached()}}.
A cell whose ML fit fails keeps its hierarchical rows, is recorded
with \code{status = "error"} in \code{ml_cells}, and is named in a warning at
the end; its sidecar is written without ML rows, so a resume tries the
ML fit again. There is no ML preflight: a compile error of the
no-pooling model shows up as every cell's ML status.

Within a cell, a subject whose ML estimate leaves the link-scale range
keeps its row with \code{estimate = NA} and \code{converged = FALSE}, as
\code{\link[=fit_ml]{fit_ml()}} documents; the recovery rows carry that verdict per subject
and term, and \code{ml_cells} counts the subjects per cell. Report \code{n} next
to any comparison of the two estimators.
}

\section{Components}{

With \code{model} a list of \code{\link[=recovery_component]{recovery_component()}}s (or a \verb{function(row)}
returning one, the same components by name in every row), each cell
simulates one set with \code{\link[=simulate_components]{simulate_components()}} under the cell's seed
and fits every component on its own through \code{\link[=fit_cached]{fit_cached()}}. \code{pars},
\code{sds}, \code{generator}, \code{tasks}, \code{task_col}, \code{re_cor} and \code{formula} belong to
the components and are errors here; \code{cors}, \code{covariates}, \code{seed},
\code{reps}, \code{scale}, \code{levels}, \code{correlations}, \code{cor_scale}, \code{smoke},
\code{preflight} and \code{...} keep their meaning, and \code{prior} is \code{NULL} or a
list by component, as in \code{\link[=fit_components]{fit_components()}}. \code{subjects = "fixed"} is not
supported for components yet. A component cannot be named \code{sim}, \code{est},
\code{cor} or \code{sd}.

Grid columns: \code{n_subjects}; \verb{n_trials_<comp>} for a component's number
of trials (a plain \code{n_trials} column is an error); \verb{<comp>_<par>} or
\verb{<comp>_<par>_<task_col><level>} for population values, a bare parameter
setting every task and a full term overriding it; \verb{sd_<comp>_<par>} for
SDs; \verb{cor_<a>__<b>} with prefixed terms or covariates, in either order.
They apply to numeric \code{pars} and \code{sds} of a component and to the result
of a function-valued one. A missing value (\code{NA}) leaves the component's
value, so a column may name a task that only some rows have. A column
whose component or term is unknown is an error; so is a column whose
prefix is no component but whose remainder is a term of one, such as a
misspelt component name. Other columns are left for \code{model} and \code{cors}
functions.

Files per cell: \verb{cell-<row>-rep-<rep>-sim.rds} holds the set;
\verb{cell-<row>-rep-<rep>-<comp>.rds} (with its \code{.key}) each fit; a sidecar
\verb{cell-<row>-rep-<rep>-<comp>-est.rds} per component holds its estimates
and, with \code{"model"} requested, its model correlations; and
\verb{cell-<row>-rep-<rep>-cor.rds} holds what needs every fit, the
correlations of \code{\link[=extract_correlations]{extract_correlations()}} on the set of fits (covariates
included) and the subject means, with the components' keys. A resume in
which every sidecar matches reads no fit; otherwise the fits are obtained
through \code{\link[=fit_cached]{fit_cached()}}, which reuses cached ones, and the stale sidecars
are rewritten. The preflight fits every component of the first cell
into \verb{<dir>/preflight-<comp>}, and is skipped when each of them has a
cached fit or a usable sidecar.

The result is one \code{bmmtools_recovery} with prefixed terms (\code{a_kappa},
\code{b_c_task1}); \code{converged} is that component fit's verdict, and
\code{scale = "natural"} uses every component's links. The \code{cells} attribute
has one row per cell and component, with a \code{component} column; the
\code{correlations} and \code{subject_means} attributes use prefixed terms, so
\code{\link[=subject_table]{subject_table()}} works on the result. A component whose fit fails is
recorded as \code{"error"} for that cell, the cell's other components are
still scored, its correlations and subject means are skipped, and the
grid goes on.

Correlations between parameters of separately fitted components are
attenuated by the reliabilities of both estimates; see "Separate fits"
in \code{\link[=extract_correlations]{extract_correlations()}}. The \code{"model"} estimator works within one
fit only and needs a component \code{formula} with correlated terms.
}

\examples{
\dontrun{
out <- recovery_grid(
  bmm::mixture2p(resp_error = "y"),
  grid = expand.grid(n_subjects = c(20, 50), n_trials = c(30, 100)),
  pars = c(kappa = log(8), thetat = qlogis(0.75)),
  sds = c(kappa = 0.3, thetat = 0.5),
  dir = "fits/mixture2p", reps = 10, seed = 1,
  chains = 4, iter = 2000, backend = "cmdstanr"
)
summary(out)
}

}
