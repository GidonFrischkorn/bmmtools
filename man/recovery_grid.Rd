% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{recovery_grid}
\alias{recovery_grid}
\title{Run a parameter-recovery grid}
\usage{
recovery_grid(
  model,
  grid,
  pars,
  dir,
  reps = 1,
  sds = NULL,
  cors = NULL,
  covariates = NULL,
  formula = NULL,
  prior = NULL,
  generator = NULL,
  seed = NULL,
  subjects = c("redraw", "fixed"),
  re_cor = c("none", "all"),
  scale = c("natural", "link"),
  smoke = FALSE,
  preflight = TRUE,
  ...,
  .fitter = NULL
)
}
\arguments{
\item{model}{A \code{bmmodel}, or a function of one grid row (a one-row
data frame) returning one, for models whose constructor depends on
the design.}

\item{grid}{A data frame with the columns \code{n_subjects} and \code{n_trials}.
A column named after a parameter gives that cell's population value
on the link scale, overriding \code{pars}; a column \verb{sd_<parameter>}
overrides \code{sds}; a column \verb{cor_<a>__<b>} sets the correlation of two
parameters or covariates (the names in either order). A SimDesign
design is a data frame and works as is.}

\item{pars, sds, cors}{Defaults for every cell, as in
\code{\link[=simulate_recovery]{simulate_recovery()}}. Each may also be a \verb{function(row)} of the
one-row grid data frame, evaluated under the cell's seed, which draws
new hyperparameters for every data set; the grid columns above are
applied to its result.}

\item{dir}{Directory for the per-cell files; created if missing.}

\item{reps}{Replications per cell.}

\item{covariates}{As in \code{\link[=simulate_recovery]{simulate_recovery()}}, the same for every cell.}

\item{formula}{A \code{bmmformula}; \code{NULL} means \code{\link[=recovery_formula]{recovery_formula()}} of the
cell's model.}

\item{prior}{Passed to \code{\link[=fit_cached]{fit_cached()}}.}

\item{generator}{As in \code{\link[=simulate_recovery]{simulate_recovery()}}.}

\item{seed}{Master seed. Each cell derives its own from it and the
cell's row and replication, so a cell is reproducible on its own.
\code{NULL} leaves everything unseeded and records \code{NA}.}

\item{subjects}{\code{"redraw"} draws new subject values in every
replication; \code{"fixed"} draws them once per row and reuses them, so
replications become a simulated retest of the same people. The
realised population values, SDs, correlations and covariate values of
replication 1 are reused too.}

\item{re_cor}{Passed to \code{\link[=recovery_formula]{recovery_formula()}} when \code{formula} is \code{NULL}.}

\item{scale}{The scale recovery is scored on, as in \code{\link[=recover]{recover()}};
natural by default.}

\item{smoke}{\code{TRUE} runs the first two rows with two replications
into \verb{<dir>/smoke}, so a smoke run never overwrites a full one.}

\item{preflight}{Run the first cell once with one chain and 200
iterations into \verb{<dir>/preflight} before the loop. Skipped when the
first cell already has a cached fit.}

\item{...}{Passed to \code{\link[=fit_cached]{fit_cached()}} and so to the fitter: \code{chains},
\code{iter}, \code{warmup}, \code{backend}, \code{cores}, ...}

\item{.fitter}{As in \code{\link[=fit_cached]{fit_cached()}}.}
}
\value{
A \code{bmmtools_recovery} object over every cell that produced a
fit, at both the population and the subject level, with a
\code{condition} column (\code{"row-<i>"}) naming the grid row. Two
attributes: \code{cells}, a tibble with one row per cell (\code{condition},
\code{replication}, \code{n_subjects}, \code{n_trials}, \code{seed}, \code{file}, \code{status},
\code{elapsed} in seconds, \code{converged}), and \code{grid}. \code{summary()} groups
by condition.
}
\description{
The loop the validation scripts in bmm wrote by hand, with the
requirements their overnight runs taught (decision 19): one durable
file per cell written as soon as the cell finishes, a resume that
reads those files and needs no temporary state, a smoke mode in its
own directory, a preflight fit that catches a compile or init error
before any cell runs, and cells ordered so that the first completed
block spans the design rather than exhausting one level.
}
\details{
A cell whose fit errors is recorded with \code{status = "error"} and the
grid goes on; a warning at the end names the failed cells. A cell
whose data cannot be generated stops the grid, because that is a
design error rather than a sampler accident. Cell files are
\verb{cell-<row>-rep-<rep>-sim.rds} (data and truth) and
\verb{cell-<row>-rep-<rep>.rds} with its \code{.key} (the fit, through
\code{\link[=fit_cached]{fit_cached()}}); a resume reads an existing simulation file rather
than regenerating, so a cached fit and its truth always match.
}
\examples{
\dontrun{
out <- recovery_grid(
  bmm::mixture2p(resp_error = "y"),
  grid = expand.grid(n_subjects = c(20, 50), n_trials = c(30, 100)),
  pars = c(kappa = log(8), thetat = qlogis(0.75)),
  sds = c(kappa = 0.3, thetat = 0.5),
  dir = "fits/mixture2p", reps = 10, seed = 1,
  chains = 4, iter = 2000, backend = "cmdstanr"
)
summary(out)
}

}
