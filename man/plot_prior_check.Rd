% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_prior_check}
\alias{plot_prior_check}
\title{Plot what the priors say the data should look like}
\usage{
plot_prior_check(
  x,
  type = c("density", "histogram", "statistic"),
  observed = TRUE,
  draws = 50,
  facet_by = "response",
  ...
)
}
\arguments{
\item{x}{A \code{bmmtools_prior_check} object from \code{\link[=prior_check]{prior_check()}}.}

\item{type}{\code{"density"}, \code{"histogram"} or \code{"statistic"}.}

\item{observed}{Overlay the observed response from the data the check
was run over. Dropped without comment when the model's response
column is not in that data, since a prior check over a design
skeleton has nothing observed to show. Ignored by \code{"statistic"}.}

\item{draws}{Prior-predictive draws to show per set, capped at what
the object holds.}

\item{facet_by}{A column of \code{x} to make panels from, or \code{NULL}.
\code{"response"} by default, so a model with several observables does
not share an axis.}

\item{...}{Not used.}
}
\value{
A \code{ggplot} object.
}
\description{
The picture a prior check is read through. \code{"density"} draws each
prior-predictive draw as its own thin line, so the spread \emph{between}
draws stays visible where a single pooled density would hide it;
\code{"histogram"} is for a discrete response, where a density is
misleading; \code{"statistic"} plots the summary table instead of the
distribution.
}
\examples{
\dontshow{if (rlang::is_installed("ggplot2")) withAutoprint(\{ # examplesIf}
plot_prior_check(prior_check_sdt_yn, type = "histogram")
plot_prior_check(prior_check_sdt_yn, type = "statistic")
\dontshow{\}) # examplesIf}
}
