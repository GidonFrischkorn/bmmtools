% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{recovery_formula}
\alias{recovery_formula}
\title{The default recovery formula: every free parameter gets a random intercept}
\usage{
recovery_formula(
  model,
  group = "id",
  re_cor = c("none", "within", "all"),
  task_col = NULL,
  coding = c("cell", "contrast")
)
}
\arguments{
\item{model}{A \code{bmmodel}.}

\item{group}{The grouping variable, \code{"id"} by default.}

\item{re_cor}{Which random effects are correlated. \code{"none"} gives
independent random effects: \code{(1 | id)}, or \code{(0 + task || id)} with
tasks. \code{"within"} correlates the tasks of each parameter,
\code{(0 + task | id)}, and needs \code{task_col}. \code{"all"} gives one correlation
matrix across every parameter (and task), \code{(1 | p | id)} or
\code{(0 + task | p | id)}, which is what the \code{model} estimator of
correlation recovery reads.}

\item{task_col}{\code{NULL}, or the task column of a simulation with \code{tasks}
(see \code{\link[=simulate_recovery]{simulate_recovery()}}). Each free parameter then gets one
population value per task, \verb{<parameter> ~ 0 + task}, and one
subject-level effect per task: cell means, whose terms match the truth
of the simulation.}

\item{coding}{How the tasks enter the formula. \code{"cell"}, the default,
is cell means, \verb{<parameter> ~ 0 + task}: one population value per
task. \code{"contrast"} writes \verb{<parameter> ~ 1 + task} instead, an
intercept and a contrast per task beyond the first, which is what a
study of \emph{effects} wants. It needs \code{task_col}. The contrast matrix
itself is carried by the data, not by the formula: give it to
\code{\link[=simulate_recovery]{simulate_recovery()}} or \code{\link[=recovery_grid]{recovery_grid()}} as \code{contrasts}, which
attaches it to the task column so brms builds the matching design.}
}
\value{
A \code{bmmformula}.
}
\description{
The formula the validation scripts in bmm converged on:
\verb{<parameter> ~ 1 + (1 | id)} for every parameter the model estimates,
and nothing for the ones it fixes. \code{\link[=recovery_grid]{recovery_grid()}} uses it when no
formula is given; a user who wants to change one term can start
from it.
}
\examples{
\dontrun{
recovery_formula(bmm::mixture2p(resp_error = "y"))
recovery_formula(bmm::mixture2p(resp_error = "y"), re_cor = "all")
recovery_formula(
  bmm::mixture2p(resp_error = "y"),
  re_cor = "within", task_col = "task"
)
}

}
