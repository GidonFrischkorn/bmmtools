% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-info.R
\name{run_info}
\alias{run_info}
\title{The machine and package versions this session runs on}
\usage{
run_info()
}
\value{
A one-row tibble: \code{r_version}, \code{platform}, \code{hostname}, \code{cores}
(\code{\link[parallel:detectCores]{parallel::detectCores()}}), \code{cpu_model}, \code{memory_gb}, the installed
versions of bmm, brms, cmdstanr, posterior and bmmtools,
\code{cmdstan_version} (from \code{cmdstanr::cmdstan_version()} when cmdstanr
is installed and does not error), and \code{timestamp}.
}
\description{
One row, meant for the runtime table of a study's parameter-recovery
page: what ran the fits, not what they estimated. Never errors --- a
source it cannot read (no \verb{/proc/cpuinfo}, no CmdStan installation,
\code{cmdstanr} not installed) degrades to \code{NA} rather than stopping the
call, because a runtime record is exactly the thing wanted after a run
that partly failed.
}
\examples{
run_info()

}
