% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{summary.bmmtools_cor_recovery}
\alias{summary.bmmtools_cor_recovery}
\alias{summary.bmmtools_cor_recovery_summary}
\title{Summarise a correlation recovery into per-pair metrics}
\usage{
\method{summary}{bmmtools_cor_recovery}(object, ...)

\method{summary}{bmmtools_cor_recovery_summary}(object, ...)
}
\arguments{
\item{object}{A \code{bmmtools_cor_recovery} object from
\code{\link[=recover_correlations]{recover_correlations()}}.}

\item{...}{Not used.}
}
\value{
A \code{bmmtools_cor_recovery_summary} tibble with the columns
\code{term}, \code{estimator}, \code{scale}, \code{n_replications}, \code{n_converged},
\code{true_value}, \code{sample_sd}, \code{mean_estimate}, \code{bias}, \code{rmse},
\code{bias_sample}, \code{rmse_sample}, \code{coverage}, \code{coverage_sample},
\code{ci_width}, \code{rejection_rate}, \code{false_positive_rate}, \code{power}, \code{r},
\code{r_low}, \code{r_high}, \code{ccc}, \code{ccc_low} and \code{ccc_high}, preceded by
\code{condition} when the object came from a grid.
}
\description{
One row per condition, pair, estimator and scale, summarised across
replications. Errors, coverage and the correlations are reported
against both truths: the generating correlation (\code{bias}, \code{rmse},
\code{coverage}) and the correlation the simulated subjects had
(\code{bias_sample}, \code{rmse_sample}, \code{coverage_sample}, \code{r}, \code{ccc}).
}
\details{
\code{true_value} is the generating correlation when it is the same in
every replication and \code{NA} otherwise. \code{sample_sd} is the standard
deviation of the in-sample correlations across replications.

\code{rejection_rate} is the share of intervals that exclude zero. It is
reported as \code{false_positive_rate} when every \code{true_value} is 0 and as
\code{power} when none is. A \code{true_value} that is \code{NA}, which is what a
nonzero correlation becomes on the natural scale, counts as not 0, so
\code{power} is defined there. With a mix of zero and nonzero values both
are \code{NA}.

\code{r} (with a Fisher-z interval) and \code{ccc} (Lin's concordance, see
\code{\link[=recovery_ccc]{recovery_ccc()}}) compare the estimates with \code{sample_value} across
replications, one pair per replication. They are \code{NA} with fewer than
three replications or when either side has no spread.
}
