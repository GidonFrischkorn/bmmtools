% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/components.R
\name{simulate_components}
\alias{simulate_components}
\title{Simulate several models for the same simulated people}
\usage{
simulate_components(
  components,
  n_subjects,
  cors = NULL,
  covariates = NULL,
  seed = NULL
)
}
\arguments{
\item{components}{A list of \code{\link[=recovery_component]{recovery_component()}}s. If the list has
names, they must be the component names.}

\item{n_subjects}{The number of subjects, shared by every component.}

\item{cors}{A correlation matrix over prefixed terms (\verb{<name>_<term>},
such as \code{m3_c_task1}) and covariates; terms it does not name are
uncorrelated. \code{NULL} means none are correlated. May be a function with
no arguments, as in \code{\link[=simulate_recovery]{simulate_recovery()}}.}

\item{covariates}{Observed, error-free person variables drawn with the
parameters, as in \code{\link[=simulate_recovery]{simulate_recovery()}}. A covariate name must differ
from every component name and from every model's parameters and
columns.}

\item{seed}{A seed applied with \code{withr::with_seed()} around everything
that is drawn; \code{NULL} leaves the random number generator alone.}
}
\value{
A list of class \code{bmmtools_simulation_set} with
\itemize{
\item \code{components}: a named list of \code{bmmtools_simulation}s, one per
component, with its own unprefixed terms, no covariates and \code{seed}
\code{NA};
\item \code{truth}: the tables of \code{\link[=simulate_recovery]{simulate_recovery()}} (\code{population},
\code{subjects}, \code{sd}, \code{cor}, \code{covariates}) over every component, with
prefixed terms; \code{cor} holds every pair of varying terms and
covariates;
\item \code{pars}, \code{sds}: the realised values, prefixed and expanded over tasks;
\item \code{cors}: the realised correlation matrix over the varying terms, then
the covariates (\code{NULL} with fewer than two);
\item \code{covariates} and \code{covariate_data}, a tibble with \code{id} and one column
per covariate;
\item \code{links}: every model's link table under prefixed names;
\item \code{specs}: the components as given;
\item \code{n_subjects} and \code{seed} (\code{NA} when none).
}
}
\description{
Draws the subject values of every component, and any covariates, from
one multivariate normal distribution, then simulates each component's
data with its own model and generator. Parameters of different models
can therefore be correlated across subjects, which is what a study of
individual differences across tasks or models needs.
}
\details{
\strong{Order of the draws.} Inside the seed, each component's \code{pars} and then
\code{sds} are evaluated, in component order, then \code{cors}. The subject values
of all components are one draw, \code{Z \%*\% chol(cors)}, over the varying
terms, component by component. The generators then run in component
order, and the covariates are drawn last, so adding a covariate never
changes the responses. A set with one component and no covariates gives
exactly the simulation \code{\link[=simulate_recovery]{simulate_recovery()}} gives with the same
arguments and seed. Adding a component after another leaves the first
one's subject values unchanged, but not its responses, because the draw
for the added terms comes before the generators.

\strong{Covariates} are not added to the components' data; they are in
\code{covariate_data} and in \code{truth$covariates}.

\strong{What separate fits can recover.} Each component is fitted on its own
by \code{\link[=fit_components]{fit_components()}}, so no fit knows about the correlation between its
parameters and another model's. Correlating the estimates across fits
attenuates the correlation by the reliabilities \code{rel_a} and \code{rel_b} of
the two subject estimates: the \code{point} estimator of
\code{\link[=extract_correlations]{extract_correlations()}} is roughly \code{rho * sqrt(rel_a * rel_b)}, and the
\code{draws} estimator roughly \code{rho * rel_a * rel_b}, so neither recovers
\code{rho}. A structural equation model of true against estimated values, from
\code{\link[=subject_table]{subject_table()}}, does; so would a joint multivariate fit of all
components, which bmmtools does not support yet.
}
\examples{
\dontrun{
components <- list(
  recovery_component(
    bmm::mixture2p(resp_error = "y"),
    pars = c(kappa = log(8), thetat = qlogis(0.75)),
    n_trials = 60, sds = c(kappa = 0.3, thetat = 0.5), name = "m2p"
  ),
  recovery_component(
    bmm::sdm(resp_error = "y"),
    pars = c(c = log(3), kappa = log(5)),
    n_trials = 60, sds = c(c = 0.4), name = "sdm"
  )
)
cors <- diag(2)
dimnames(cors) <- rep(list(c("m2p_thetat", "sdm_c")), 2)
cors[1, 2] <- cors[2, 1] <- 0.6
set <- simulate_components(components, n_subjects = 100, cors = cors,
  seed = 1
)
fits <- fit_components(set, dir = "fits")
recover_correlations(fits, set, estimator = c("draws", "point"))
}

}
