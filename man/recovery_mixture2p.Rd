% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{recovery_mixture2p}
\alias{recovery_mixture2p}
\title{Example recovery object: a mixture2p grid}
\format{
A \code{bmmtools_recovery} tibble at both the population and the
subject level, with the columns documented in \code{\link[=recover]{recover()}} and a
\code{condition} column naming the grid row. The attribute \code{cells} holds
one row per cell with its seed, status, runtime and convergence
verdict; \code{grid} holds the design.
}
\source{
\code{data-raw/example-objects.R} in the package's GitHub
repository.
}
\usage{
recovery_mixture2p
}
\description{
A precomputed \code{\link[=recovery_grid]{recovery_grid()}} result, shipped so that \code{summary()},
\code{\link[=plot_recovery]{plot_recovery()}} and dplyr verbs can be tried without compiling a
model. The grid crosses 20 and 50 subjects with 30 and 100 trials per
subject, with 5 replications per cell, for \code{\link[bmm:mixture2p]{bmm::mixture2p()}} with
population values \code{kappa = log(8)} and \code{thetat = qlogis(0.75)} on the
link scale and between-subject SDs of 0.3 and 0.5. Every fit used 4
chains of 1000 iterations with cmdstanr, and the master seed was 2026.
Scores are on the natural scale.
}
\examples{
summary(recovery_mixture2p)
attr(recovery_mixture2p, "cells")
}
\seealso{
\code{\link[=recovery_grid]{recovery_grid()}}, \code{\link[=summary.bmmtools_recovery]{summary.bmmtools_recovery()}}
}
\keyword{datasets}
