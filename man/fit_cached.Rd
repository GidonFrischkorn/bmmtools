% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{fit_cached}
\alias{fit_cached}
\title{Fit a bmm model once and reuse it while nothing has changed}
\usage{
fit_cached(
  formula,
  data,
  model,
  file,
  prior = NULL,
  refit = c("on_change", "never", "always"),
  ...,
  .fitter = NULL
)
}
\arguments{
\item{formula, data, model, prior}{Passed to the fitter as they are; the
formula enters the key deparsed, so the environment it was built in
does not matter.}

\item{file}{Path of the cache file. \code{.rds} is appended when the path
has no extension; the key is written next to it as \verb{<file>.key}. The
directory is created before fitting, so an unwritable path fails
before any sampling.}

\item{refit}{\code{"on_change"} (the default) fits when nothing is cached or
when any key component changed; \code{"never"} returns whatever is cached
and warns if it is stale; \code{"always"} fits and overwrites.}

\item{...}{Passed to the fitter. \code{seed}, \code{chains}, \code{iter}, \code{warmup},
\code{thin}, \code{control}, \code{init}, \code{backend}, \code{sample_prior}, \code{algorithm}
and \code{stanvars} enter the key, and so does \code{draws} (the number of
draws of a Laplace or variational fit) when it is given; anything
else (\code{cores}, \code{refresh}, \code{silent}, ...) changes how the fit is run,
not what it is, and does not. An \code{init} given as a function enters
the key by its text, so a
closure built afresh in every iteration of a loop still matches.
brms's \code{file_refit} and \code{file_compress} are refused: there is one
cache, not two.}

\item{.fitter}{The fitting function, \code{bmm::bmm()} by default. Tests
inject a stand-in so that no model is compiled.}
}
\value{
The fit, with an attribute \code{bmmtools_cache}: a list of
\code{file}, \code{key} and \code{reused}.
}
\description{
A cache for the fits a recovery study produces. brms's own \code{file}
argument reuses a saved fit when the Stan code, the Stan data and the
algorithm are unchanged, and does not look at the seed, the number of
chains or iterations, the warmup, thinning, \code{control}, \code{init} or the
backend (measured on brms 2.23.0). In a simulation grid the cells that
share a design differ in exactly those, so \code{fit_cached()} keys on all
of them, and on the formula, data, model, prior, the installed
versions of bmm and brms and the Stan toolchain of the backend in use,
and records each component's hash next to the fit so that a mismatch
is reported by name.
}
\details{
The fit and the key are each written to a temporary name and renamed
into place, so an interrupted write never leaves a truncated file
under the final name; the key is written after the fit. A cached fit
that cannot be read is reported as such rather than as a parse error.
}
\examples{
\dontrun{
fit <- fit_cached(
  bmm::bmf(kappa ~ 1 + (1 | id), thetat ~ 1 + (1 | id)),
  data, bmm::mixture2p(resp_error = "y"),
  file = "fits/cell-01", seed = 1, chains = 4, iter = 2000
)
attr(fit, "bmmtools_cache")$reused
}

}
